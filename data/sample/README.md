# Sample batches

These sanitized JSON files are small enough for a demo volume and intentionally contain dirty data. `batch_001.json` has eight records: five valid Silver events and three quarantined records (one missing `event_time`, one missing `track_id`, and one missing `detection_confidence`). `batch_002.json` continues a valid journey in a later batch.
